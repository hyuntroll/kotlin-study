# Kotlin 단계별 실습 문제

모든 문제는 정답 없이 문제만 정리했습니다.

실습 규칙
1. 각 문제는 새 Kotlin 코드로 작성합니다.
2. 문제마다 아래에 적힌 `실습 패키지`로 시작합니다.
3. 입력은 `Scanner` 또는 `readln()` 중 편한 방식으로 받습니다.
4. 출력 형식은 예시와 최대한 같게 맞춥니다.

## 1단계. 변수와 문자열

## 문제 1. 자기소개 업그레이드
실습 패키지: `practice.step01.variables`

학습 목표: `val`, `var`, 타입 명시, 타입 추론, 문자열 템플릿

요구사항
- 이름, 나이, 사는 도시를 입력받으시오.
- 이름은 타입을 직접 명시해서 저장하시오.
- 나이는 타입 추론으로 저장하시오.
- 도시는 `var`로 저장한 뒤, 출력 전에 `"서울"`로 한 번 변경하시오.
- 출력할 때 나이는 1 증가시켜 내년 나이로 보여주시오.

입력
- 1번째 줄: 이름
- 2번째 줄: 나이
- 3번째 줄: 현재 사는 도시

출력
- `저는 {이름}이고 내년에는 {나이+1}살입니다. 현재 등록된 도시는 {도시}입니다.`

입력 예시
```text
민수
20
부산
```

출력 예시
```text
저는 민수이고 내년에는 21살입니다. 현재 등록된 도시는 서울입니다.
```

## 문제 2. nullable 프로필 출력기
실습 패키지: `practice.step02.nullable`

학습 목표: null 가능 타입, safe call, Elvis 연산자

요구사항
- 닉네임과 나이를 입력받으시오.
- 입력값이 문자열 `"null"`이면 실제 null로 처리하시오.
- 닉네임 길이는 safe call을 사용해서 구하시오.
- 닉네임이 null이면 `"닉네임 없음"`을 출력하시오.
- 나이가 null이면 기본값 `0`으로 처리하시오.

입력
- 1번째 줄: 닉네임 또는 `null`
- 2번째 줄: 나이 또는 `null`

출력
- 1번째 줄: `닉네임: {닉네임 또는 닉네임 없음}`
- 2번째 줄: `닉네임 길이: {길이 또는 -1}`
- 3번째 줄: `나이: {나이}`

입력 예시
```text
null
null
```

출력 예시
```text
닉네임: 닉네임 없음
닉네임 길이: -1
나이: 0
```

## 문제 3. 문자열 정리기
실습 패키지: `practice.step03.string`

학습 목표: 문자열 템플릿, `uppercase()`, `lowercase()`

요구사항
- 이름 하나를 입력받으시오.
- 원본 문자열, 대문자 변환 결과, 소문자 변환 결과를 순서대로 출력하시오.
- 마지막 줄에는 문자열 템플릿을 사용해 `제 이름은 {이름}입니다.`를 출력하시오.

입력
- 1번째 줄: 이름

출력
- 1번째 줄: 원본 문자열
- 2번째 줄: 대문자 변환 결과
- 3번째 줄: 소문자 변환 결과
- 4번째 줄: 소개 문장

입력 예시
```text
Kotlin
```

출력 예시
```text
Kotlin
KOTLIN
kotlin
제 이름은 Kotlin입니다.
```

## 2단계. 조건문과 반복문

## 문제 4. 입장 가능 여부 판별기
실습 패키지: `practice.step04.ifexpr`

학습 목표: `if` 표현식

요구사항
- 나이와 키를 입력받으시오.
- `if`를 표현식으로 사용해서 `입장 가능` 또는 `입장 불가`를 하나의 변수에 저장하시오.
- 조건은 나이가 15세 이상이고 키가 140 이상일 때만 입장 가능이다.

입력
- 1번째 줄: 나이
- 2번째 줄: 키

출력
- `판정 결과: {결과}`

입력 예시
```text
17
168
```

출력 예시
```text
판정 결과: 입장 가능
```

## 문제 5. 요일 판별기
실습 패키지: `practice.step05.whenvalue`

학습 목표: `when (값)` 형태

요구사항
- 요일 문자열을 입력받으시오.
- `when`을 사용해서 아래 기준으로 출력하시오.
- `"Sunday"` 또는 `"sunday"`이면 `주말`
- `"Friday"`이면 `불금`
- `"Mon"`, `"Tue"`, `"Wed"`, `"Thu"`, `"Fri"`, `"Sat"` 중 하나이면 `평일 또는 토요일 축약형`
- 그 외에는 `알 수 없음`

입력
- 1번째 줄: 요일 문자열

출력
- 판별 결과 한 줄

입력 예시
```text
Friday
```

출력 예시
```text
불금
```

## 문제 6. 온도 구간 분류기
실습 패키지: `practice.step06.whenexpr`

학습 목표: `when { ... }` 형태, 범위 비교

요구사항
- 온도를 정수로 입력받으시오.
- `when { ... }` 형태를 사용해서 아래와 같이 분류하시오.
- 0도 미만이면 `매우 추움`
- 0도 이상 10도 이하면 `추움`
- 11도 이상 25도 이하면 `적당함`
- 26도 이상이면 `더움`

입력
- 1번째 줄: 온도

출력
- 분류 결과 한 줄

입력 예시
```text
18
```

출력 예시
```text
적당함
```

## 문제 7. 숫자 범위 출력기
실습 패키지: `practice.step07.loop`

학습 목표: `for`, `..`, `downTo`, `step`

요구사항
- 시작 숫자, 끝 숫자, 간격을 입력받으시오.
- 시작 숫자가 끝 숫자보다 작거나 같으면 오름차순으로 출력하시오.
- 시작 숫자가 끝 숫자보다 크면 내림차순으로 출력하시오.
- 출력은 공백으로 구분하시오.

입력
- 1번째 줄: 시작 숫자
- 2번째 줄: 끝 숫자
- 3번째 줄: 간격

출력
- 범위를 따라 출력된 숫자들

입력 예시
```text
10
2
2
```

출력 예시
```text
10 8 6 4 2
```

## 3단계. 클래스와 프로퍼티

## 문제 8. 학생 소개 객체 만들기
실습 패키지: `practice.step08.classbasic`

학습 목표: 클래스, 기본 생성자, `init`

요구사항
- 이름과 나이를 입력받아 학생 객체를 만드시오.
- 객체가 생성될 때 `init` 블록에서 `{이름} 학생이 생성되었습니다.`를 출력하시오.
- 별도의 소개 함수를 만들어 `안녕하세요. 저는 {이름}이고 {나이}살입니다.`를 출력하시오.

입력
- 1번째 줄: 이름
- 2번째 줄: 나이

출력
- 1번째 줄: 생성 메시지
- 2번째 줄: 소개 메시지

입력 예시
```text
지우
16
```

출력 예시
```text
지우 학생이 생성되었습니다.
안녕하세요. 저는 지우이고 16살입니다.
```

## 문제 9. 사용자 정보 정리기
실습 패키지: `practice.step09.property`

학습 목표: custom getter, custom setter, `private set`, validation

요구사항
- 이름과 학번을 입력받으시오.
- 이름 프로퍼티는 setter에서 앞뒤 공백을 제거해서 저장하시오.
- 학번 프로퍼티는 외부에서 직접 수정할 수 없게 만드시오.
- 학번을 바꾸는 별도 함수를 만들고, 학번이 `0`으로 시작하면 예외 대신 `잘못된 학번입니다.`를 출력하시오.
- 정상 학번이면 변경 후 최종 정보를 출력하시오.

입력
- 1번째 줄: 이름
- 2번째 줄: 현재 학번
- 3번째 줄: 변경할 학번

출력
- 학번이 정상일 때: 정리된 이름과 최종 학번
- 학번이 비정상이면: 오류 메시지

입력 예시
```text
   유나   
1323
0132
```

출력 예시
```text
잘못된 학번입니다.
```

## 문제 10. 사각형 넓이 계산기
실습 패키지: `practice.step10.getter`

학습 목표: 계산 프로퍼티, getter

요구사항
- 가로와 세로를 입력받으시오.
- `area`를 함수가 아니라 읽기 전용 프로퍼티로 구현하시오.
- 출력은 `넓이: {값}` 형식으로 하시오.

입력
- 1번째 줄: 가로
- 2번째 줄: 세로

출력
- `넓이: {값}`

입력 예시
```text
10
20
```

출력 예시
```text
넓이: 200
```

## 4단계. 데이터 중심 문법

## 문제 11. 회원 복사 실습
실습 패키지: `practice.step11.dataclass`

학습 목표: `data class`, `copy`, `==`, `===`

요구사항
- 이름, 나이, 원래 ID, 새 ID를 입력받으시오.
- 데이터 클래스로 회원 객체를 하나 만드시오.
- `copy`를 사용해서 ID만 바뀐 새 객체를 만드시오.
- 원본 객체를 그대로 참조하는 변수도 하나 만드시오.
- 아래 비교 결과를 출력하시오.
- 원본과 복사본의 `==`
- 원본과 복사본의 `===`
- 원본과 같은 참조를 가진 변수의 `===`

입력
- 1번째 줄: 이름
- 2번째 줄: 나이
- 3번째 줄: 원래 ID
- 4번째 줄: 새 ID

출력
- 1번째 줄: 원본 객체 정보
- 2번째 줄: 복사본 객체 정보
- 3번째 줄: `원본 == 복사본: {결과}`
- 4번째 줄: `원본 === 복사본: {결과}`
- 5번째 줄: `원본 === 같은참조: {결과}`

입력 예시
```text
하린
18
1
2
```

출력 예시
```text
원본: Member(name=하린, age=18, id=1)
복사본: Member(name=하린, age=18, id=2)
원본 == 복사본: false
원본 === 복사본: false
원본 === 같은참조: true
```

## 문제 12. 좌표 구조 분해
실습 패키지: `practice.step12.destructuring`

학습 목표: `componentN`, 구조 분해 선언

요구사항
- x와 y를 입력받으시오.
- 일반 클래스로 좌표 객체를 만들고, 구조 분해 선언이 가능하도록 구현하시오.
- 구조 분해한 값을 사용해서 `x={값}, y={값}` 형식으로 출력하시오.
- 마지막 줄에는 두 값의 합도 출력하시오.

입력
- 1번째 줄: x
- 2번째 줄: y

출력
- 1번째 줄: 좌표 출력
- 2번째 줄: 합계 출력

입력 예시
```text
10
20
```

출력 예시
```text
x=10, y=20
sum=30
```

## 5단계. 인터페이스와 상속

## 문제 13. 설명 가능한 상품 만들기
실습 패키지: `practice.step13.interfacebasic`

학습 목표: 인터페이스 프로퍼티, 메서드 오버라이드

요구사항
- 상품 이름, 색상, 긴급 여부를 입력받으시오.
- 이름과 색상을 가지는 인터페이스를 정의하시오.
- 설명 문자열을 반환하는 메서드를 인터페이스에 선언하시오.
- 구현 클래스에서 메서드를 오버라이드해 `{이름}, {긴급여부}, {색상}` 형식으로 반환하시오.

입력
- 1번째 줄: 상품 이름
- 2번째 줄: 색상
- 3번째 줄: 긴급 여부 (`true` 또는 `false`)

출력
- 설명 문자열 한 줄

입력 예시
```text
노트북
silver
true
```

출력 예시
```text
노트북, true, silver
```

## 문제 14. 에너지 생명체 시뮬레이터
실습 패키지: `practice.step14.inheritance`

학습 목표: 추상 클래스, 다중 인터페이스 구현, 상속, `override`, `super`

요구사항
- `run()`과 `sleep()`을 가지는 인터페이스를 정의하시오.
- 이름과 에너지를 가지는 추상 클래스를 만들고, `run()`은 기본 구현을 제공하시오.
- 사람 클래스를 만들어 `sleep()`과 `fillEnergy()`를 구현하시오.
- 입력받은 보너스 에너지만큼 충전한 뒤, `run -> sleep -> run` 순서로 실행하시오.
- 마지막 줄에는 최종 에너지를 출력하시오.

입력
- 1번째 줄: 이름
- 2번째 줄: 초기 에너지
- 3번째 줄: 보너스 에너지

출력
- 실행 과정 로그
- 마지막 줄: `final energy = {에너지}`

입력 예시
```text
Human
3
5
```

출력 예시
```text
Human is running...
Human is sleeping...
Human is running...
final energy = 26
```

## 6단계. 람다와 고차 함수

## 문제 15. 직접 만드는 forEach
실습 패키지: `practice.step15.lambda`

학습 목표: 함수 타입, trailing lambda, 제네릭 고차 함수

요구사항
- 숫자 개수와 숫자 목록을 입력받으시오.
- 직접 `forEachCustom` 함수를 만드시오.
- `forEachCustom`는 제네릭 함수여야 한다.
- 각 숫자를 순회하면서 2배 값을 한 줄씩 출력하시오.
- 모든 출력이 끝난 뒤 마지막 줄에 `done`을 출력하시오.

입력
- 1번째 줄: 숫자 개수
- 2번째 줄: 공백으로 구분된 숫자들

출력
- 각 숫자의 2배를 한 줄씩 출력
- 마지막 줄: `done`

입력 예시
```text
5
1 2 3 4 5
```

출력 예시
```text
2
4
6
8
10
done
```

## 문제 16. 시작 알림 콜백
실습 패키지: `practice.step16.callback`

학습 목표: `() -> Unit`, 기본 람다, trailing lambda

요구사항
- 작업 이름 하나를 입력받으시오.
- `announce`라는 함수를 만들고, 이 함수는 기본값이 빈 람다인 콜백을 하나 받으시오.
- `announce`는 먼저 `작업을 시작합니다.`를 출력하고, 그 다음 콜백을 실행하시오.
- 콜백에서는 `{작업 이름} 실행 중`을 출력하시오.

입력
- 1번째 줄: 작업 이름

출력
- 1번째 줄: 시작 메시지
- 2번째 줄: 작업 실행 메시지

입력 예시
```text
정산
```

출력 예시
```text
작업을 시작합니다.
정산 실행 중
```

## 7단계. object, 확장, enum

## 문제 17. 에너지 서버 관리자
실습 패키지: `practice.step17.object`

학습 목표: `object`, 상태 공유, `companion object`

요구사항
- 충전 횟수와 실행 횟수를 입력받으시오.
- 싱글톤 객체로 서버 관리자를 만드시오.
- 충전 함수는 호출될 때마다 에너지를 1 증가시키시오.
- 실행 함수는 에너지가 1 이상일 때만 실행되고, 실행될 때마다 에너지를 1 감소시키시오.
- 별도의 클래스 하나를 만들고, 그 안에 `companion object` 상수 `DNA`를 선언하시오.
- 모든 실행이 끝난 뒤 마지막 줄에 `DNA`를 출력하시오.

입력
- 1번째 줄: 충전 횟수
- 2번째 줄: 실행 횟수

출력
- 서버 실행 또는 실패 로그
- 마지막 줄: `DNA: {상수값}`

입력 예시
```text
2
3
```

출력 예시
```text
server is running...
server is running...
server has to get more energy
DNA: AAAAAGGTCGAGTCAGGGTACCAGTAC
```

## 문제 18. 문자열 끝 문자 바꾸기
실습 패키지: `practice.step18.extension`

학습 목표: 확장 함수, 확장 프로퍼티

요구사항
- 문자열 하나와 바꿀 문자 하나를 입력받으시오.
- 문자열을 `<<문자열>>` 형태로 감싸서 반환하는 확장 함수를 작성하시오.
- `StringBuilder`의 마지막 문자를 읽고 수정할 수 있는 확장 프로퍼티를 작성하시오.
- 원본 문자열의 꾸민 결과와, 마지막 문자가 바뀐 결과를 순서대로 출력하시오.

입력
- 1번째 줄: 문자열
- 2번째 줄: 바꿀 문자

출력
- 1번째 줄: 꾸민 문자열
- 2번째 줄: 마지막 문자가 바뀐 문자열

입력 예시
```text
Char
z
```

출력 예시
```text
<<Char>>
Chaz
```

## 문제 19. 결제 카드 처리기
실습 패키지: `practice.step19.enumclass`

학습 목표: `enum class`, 생성자 프로퍼티, 상수별 오버라이드, 인터페이스 구현

요구사항
- 카드 종류를 입력받으시오.
- 카드 종류는 `VISA`, `MASTERCARD`, `DISCOVER` 중 하나이다.
- 각 카드 종류는 서로 다른 `protocolId`를 가져야 한다.
- 각 카드 종류는 `pay()` 동작을 다르게 구현하시오.
- 출력은 카드 이름, 프로토콜 번호, 결제 메시지 순서로 하시오.
- 추가 실습으로 NFC 카드용 enum을 하나 더 만들고, 인터페이스를 구현해 번호를 반환하도록 하시오.

입력
- 1번째 줄: 카드 종류

출력
- 1번째 줄: 카드 이름
- 2번째 줄: 프로토콜 번호
- 3번째 줄: 결제 메시지

입력 예시
```text
VISA
```

출력 예시
```text
VISA
12314
visa로 결제합니다!!
```

## 8단계. sealed로 상태 모델링

## 문제 20. API 응답 처리기
실습 패키지: `practice.step20.sealedclass`

학습 목표: `sealed class`, `when`, 상태 모델링

요구사항
- 응답 상태를 입력받으시오.
- 상태는 `success`, `failure`, `loading` 중 하나이다.
- `sealed class`로 응답 타입을 설계하시오.
- 성공은 문자열 값을 가져야 한다.
- 실패는 에러 메시지를 가져야 한다.
- 로딩은 `object`로 표현하시오.
- `when`을 사용해 상태별 출력 문장을 작성하시오.
- `else` 없이 모든 분기를 처리하시오.

입력
- 1번째 줄: 상태
- 2번째 줄: 성공 값 또는 실패 메시지

입력 예시
```text
success
야르
```

출력 예시
```text
성공: 야르
```

## 문제 21. 결과 타입 확장기
실습 패키지: `practice.step21.sealedinterface`

학습 목표: `sealed interface`, 여러 구현 타입, `when`

요구사항
- 결과 상태를 입력받으시오.
- 상태는 `success`, `failure` 중 하나이다.
- `sealed interface`를 만들고, 성공 타입과 실패 타입을 각각 구현하시오.
- 추가로 별도 인터페이스 하나를 만들고, 성공 타입 또는 실패 타입 중 하나가 함께 구현하도록 확장하시오.
- `when`으로 결과를 처리하시오.

입력
- 1번째 줄: 상태
- 2번째 줄: 값 또는 실패 사유

출력
- 성공이면 `성공했습니다. 값: {값}`
- 실패면 `실패했습니다. 사유: {사유}`

입력 예시
```text
failure
네트워크 오류
```

출력 예시
```text
실패했습니다. 사유: 네트워크 오류
```

## 9단계. 추가 실습

## 문제 22. 이름과 나이 입력 프로그램
실습 패키지: `practice.step22.input`

학습 목표: 콘솔 입력, `Scanner`, 문자열과 숫자 조합 출력

요구사항
- 이름과 나이를 입력받으시오.
- 입력은 `Scanner`를 사용해 구현하시오.
- 출력은 `{이름} is {나이} years old` 형식으로 하시오.
- 가능하면 줄 입력과 숫자 입력을 함께 쓸 때 버퍼가 꼬이지 않도록 스스로 정리해 보시오.

입력
- 1번째 줄: 이름
- 2번째 줄: 나이

출력
- 한 줄 출력

입력 예시
```text
Kotlin
4
```

출력 예시
```text
Kotlin is 4 years old
```
